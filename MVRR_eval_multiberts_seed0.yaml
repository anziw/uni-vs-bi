# evaluate on unidirectional models * 25
exp: MinimalPair

mode: 
    - evaluate

models: 
      hf_masked_model:
          - google/multiberts-seed_0

datafpath: data/MVRR_formatted.tsv
predfpath: evaluations/MVRR_eval_multiberts_seed0.tsv

batchSize: 256